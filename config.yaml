# Oiduna Configuration

# Authentication
auth:
  admin_password: "change_me_in_production"

# API Server
api:
  host: "0.0.0.0"
  port: 57122

# Loop Engine
loop:
  steps: 256  # Fixed (do not change)

# Destinations file path
destinations_file: "destinations.yaml"
